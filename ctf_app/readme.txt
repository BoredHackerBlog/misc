Python flask app

CTF app that has no point system or login.

config.py contains flag and the file to return when the flag is entered.

Goal was to let people find flags, when they enter it and a file is returned to them with the next "quest"/task.